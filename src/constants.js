const SVR_URL = 'https://tvshowdbapi.herokuapp.com';
const STUDIO_URL = `${SVR_URL}/studios`;
const TV_SHOWS_URL = `${SVR_URL}/tvshows`;
const SHOWS_ID_URL = `${SVR_URL}/tvshow`;
const GENRES_URL = `${SVR_URL}/genres`;
const EPISODES_URL = `${SVR_URL}/episodes`;
const EPISODE_URL = `${SVR_URL}/episode`;
const SRV_AUTH = `${SVR_URL}/auth`;
const SRV_USER = `${SVR_URL}/user`;
const EPISODEPLAY_URL = `${SVR_URL}/viewepisode`;
// eslint-disable-next-line import/prefer-default-export
export {
    SVR_URL, STUDIO_URL,
    TV_SHOWS_URL,
    SHOWS_ID_URL,
    GENRES_URL,
    EPISODES_URL,
    EPISODE_URL,
    SRV_AUTH,
    SRV_USER,
    EPISODEPLAY_URL,
};
